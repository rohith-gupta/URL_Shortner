/**
 * JPA entities — the persistent domain model. Mapped directly to database tables; kept separate
 * from {@code dto} so the API contract and the storage schema can evolve independently.
 */
package com.urlshortener.entity;
