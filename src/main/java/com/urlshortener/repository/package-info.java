/**
 * Spring Data JPA repositories — the persistence layer. Repositories are thin data-access
 * interfaces over {@code entity} types; no business logic belongs here.
 */
package com.urlshortener.repository;
