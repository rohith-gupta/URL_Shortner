/**
 * REST controllers (the API layer). Controllers translate HTTP requests to/from service calls
 * and are responsible for request/response mapping, HTTP status codes, and OpenAPI annotations.
 * No business logic belongs here — delegate to {@code service}.
 */
package com.urlshortener.controller;
