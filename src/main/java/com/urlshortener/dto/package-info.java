/**
 * Data transfer objects — request/response shapes exposed by the API. Kept separate from
 * {@code entity} so internal persistence details are never leaked directly through the API.
 */
package com.urlshortener.dto;
