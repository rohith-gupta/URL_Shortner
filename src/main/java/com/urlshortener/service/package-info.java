/**
 * Business logic layer. Services implement the use cases (e.g., creating a short URL, resolving
 * a redirect, computing analytics) and orchestrate {@code repository} access. Controllers call
 * services; services do not depend on {@code controller} or the HTTP layer.
 */
package com.urlshortener.service;
